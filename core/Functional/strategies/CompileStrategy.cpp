#include <IStrategy.cpp>

/*
Strategy that compiles program into executable file.
*/
class CompileStrategy : public Strategy
{
private:
    /* data */
public:
    CompileStrategy(/* args */) {}
    ~CompileStrategy() {}
};